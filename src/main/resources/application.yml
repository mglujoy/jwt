
spring:
    datasource:
        url: ${DBURL}
        username: ${DBUSR}
        password: ${DBPASS}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto : update
        show-sql: true
        properties: 
            hibernate:
                format_sql: true
        database: mysql
        databse-platform: org.hibernate.dialect.MySQLDialect
    autoconfigure:
        exclude: 

